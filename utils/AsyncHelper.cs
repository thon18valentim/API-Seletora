namespace Transacoes_blockchain.utils
{
  public static class AsyncHelper
  {
    //public async static Task<List<T>> LoopAsyncResult<T>(this List<T> list, Func<string> function)
    //{
    //  var result = await Task.WhenAll(list.Select(function));

    //  return result.ToList();
    //}

    //public async static Task<List<string>> ExecuteAllPostTasks<T>(List<T> list, Func<T,> function)
    //{
    //  List<Task> tasks = new();
    //  foreach(var val in list)
    //  {
    //    tasks.Add(function(val));
    //  }
    //}
  }
}
